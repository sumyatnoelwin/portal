<%= form_for([@forum, @forum.comments.build]) do |f| %>
  <p>
    <%= f.label :student_id %><br>
    <%= f.text_field :student_id %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>