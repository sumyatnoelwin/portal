<%= simple_form_for(@forum) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :author_id, :as => :select, :collection=> Student.all.map { |s| [s.student_name,s.id]  } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
